/**
 * PLACEHOLDER — restored by scripts/restore-simulation.cjs on npm run dev / build.
 *
 * Full body lives in Simulation.ts.gz.b64.part1…partN (gzip+base64).
 * Companion modules (always on main as source):
 *   - src/core/simTypes.ts  (Unit, Building, ResourceNode, …)
 *   - src/core/simAI.ts     (SimulationAI opponent logic)
 *
 * Features in restored body: attack-move, towers, market, walls, generals/auras,
 * fog, research gates, attrition/supply, siege/capture, epochs, formation offsets.
 */
export {};

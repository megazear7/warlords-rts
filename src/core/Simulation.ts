/**
 * PLACEHOLDER — full Simulation is restored automatically by:
 *   npm run restore-sim
 *   (also runs on npm run dev / npm run build via predev/prebuild)
 *
 * Payload: src/core/Simulation.ts.gz.b64.part1 + part2 + part3
 * Features in restored body: attack-move, towers, market trades, walls (Mil2),
 * generals + auras, explored fog cells, vision radii, civic farm boost,
 * research gates (tower=Mil1, market=Com1, wall=Mil2), AI economy/waves,
 * attrition/supply, siege/capture, epochs, control groups, etc.
 */
export {};
